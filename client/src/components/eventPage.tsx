import React, { useState } from "react";
import { useMetaMaskContext } from "../contexts/metaMaskContext";
import { ethers } from "ethers";

const EventPage = ({ eventAddress }: { eventAddress: string }) => {
    
}

export default EventPage;